#include "main.h"
/**
 * mul-multiplies two intergers
 * @a: multipling
 * @b: multiplier
 * Return: the result of mutiplication
 */
int mul(int a, int b)
{
	int result;

	results = a * b;
	return (result);
}
